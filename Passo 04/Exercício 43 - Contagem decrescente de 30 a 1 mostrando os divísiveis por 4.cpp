#include <stdio.h>

int main(void) {
		
		int contador;
		
		for (contador = 30; contador >= 1; contador -= 1) {
			printf ("%i", contador);
			if (contador % 4 = 0) {
				printf ("% i", contador);
			}
		}
		
}
